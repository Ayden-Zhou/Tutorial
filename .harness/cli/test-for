#!/usr/bin/env bash
set -euo pipefail
target="${1:-}"
if [ -z "$target" ]; then
    echo "用法: test-for <test-file>" >&2
    exit 1
fi
cd /Users/yunfan/Desktop/MyProjects/Mailbox
if [ -x .venv/bin/pytest ]; then
    .venv/bin/pytest "$target" -v
else
    python3 -m pytest "$target" -v
fi
